{
  "name": "@real-wopr/terminal",
  "version": "0.1.0",
  "private": true,
  "description": "The console terminal — one protocol, two renderers (TTY here, xterm.js on the web)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"tests/*.test.ts\""
  },
  "dependencies": { "ws": "^8.18.0" },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.12",
    "typescript": "^5.7.0"
  },
  "engines": { "node": ">=23.6" }
}
